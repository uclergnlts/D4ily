// Minimal test file for debugging (JavaScript)
import { describe, it, expect } from 'vitest';

describe('Minimal JS Test', () => {
    it('should pass', () => {
        expect(1 + 1).toBe(2);
    });
});
